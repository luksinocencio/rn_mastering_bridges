Se o projeto react native estiver na mesma pasta do projeto npm, rode o seguinte comando para adicionar o pacote localmente

```
    npm install ../nome_pasta_projeto
    npm install ../package-math
```
